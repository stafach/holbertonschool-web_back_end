#!/usr/bin/env python3
"""Module that defines a function
to convert a float to a string with type annotations."""


def to_str(n: float) -> str:
    """Convert a float to a string."""
    return str(n)
